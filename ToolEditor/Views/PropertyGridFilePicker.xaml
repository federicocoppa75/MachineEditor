<UserControl x:Class="ToolEditor.Views.PropertyGridFilePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolEditor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="TheControl">
    <DockPanel>
        <Button x:Name="PickFileButton" Content="…" Click="PickFileButton_Click" DockPanel.Dock="Right" Width="15" />
        <TextBox Text="{Binding ElementName=TheControl, Path=Value}" ToolTip="{Binding ElementName=TheControl, Path=Value}"/>
    </DockPanel>
</UserControl>
